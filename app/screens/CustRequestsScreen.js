//NOTE : The main screen where a customer can insert pickup and dropoff location and choose their vehicle type,
// on selecting a vehicle they will then be taken to the order form screen where they will fill in more details about the order
//**Need API key to do this because uses Google Maps PlugIn */
import React from 'react';
import {View, Stylesheet, Text} from 'react-native';

function CustRequestsScreen(props) {
    return (
        <View >
            <Text> "Requests/Main Screen </Text>
        </View>
    );
}


export default CustRequestsScreen;